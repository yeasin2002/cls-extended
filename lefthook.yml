pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: pnpm lint
    - name: typecheck
      run: pnpm typecheck
    - name: format
      run: pnpm format
    - name: build cls-extended pkg
      run: pnpm --filter cls-extended build
